services:
  nginx:
    image: nginx:1.31.2-alpine
    container_name: nginx
    ports:
      - 80:80
      - 443:443
    restart: always
    network_mode: "host"
    volumes:
      - ./conf/:/etc/nginx/conf.d/:ro
      - ./ssl/:/etc/nginx/ssl/:ro
